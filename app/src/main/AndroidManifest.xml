<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mangal_murti_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactUsActivity"></activity>
        <activity android:name=".DonateUsActivity" />
        <activity android:name=".TrustiesActivity" />
        <activity android:name=".OtherActivitiesActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".HostelFacilitiesActivity" />
        <activity android:name=".NewsAndEventsActivity" />
        <activity android:name=".SpecialTherapyActivity" />
        <activity android:name=".MenuScreenActivity" />
        <activity
            android:name=".SplashScreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>